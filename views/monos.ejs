<%- include layouts/header.ejs %>

        <div class="page-data">
         <div class="data-btn">
           <button onClick="addMono();">+ Add</button>
         </div>
         <div class="data-table">
            <table border="1" cellpadding="7" cellspacing="7">
                <tr>
                    <th width="50px">ID</th>
                    <th>Name</th>
                    <th width="120px">Acción</th>
                </tr>

                <% if(data.length){

                 for(var i = 0;i < data.length;i++) { %>

                <tr>
                    <td><%=data[i].id%></td>
                    <td><%=data[i].nombre%></td>

                    <td>
                        <a class="a-inside edit" href="../monos/edit/<%=data[i].id%>">Edit</a>

                        <a class="a-inside delete" href="../monos/delete/<%=data[i].id%>">Delete</a>

                    </td>
                </tr>
            <% }

             }else{ %>
                 <tr>
                    <td colspan="3">No existe</td>
                 </tr>
            <% } %>



            </table>
         </div>
        </div>

<%- include layouts/footer.ejs %>
